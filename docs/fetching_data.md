# Yu-Gi-Oh Decision Evaluator — YGOPRODeck Fetching Notes

## Automation

Given a text file of card names/ids, a python file will read every line (delimited by newline) and then fetch the corresponding information.

It then will write the relevant information into a `JSON` file.
