# YGOEvaluator

## What it is
A format-scoped tool that evaluates a Yu-Gi-Oh board state, classifies the complexity/type of decision present, and optionally suggests next steps (this is currently a pipe-dream that may use ML).

Primary motivation is
lowering the learning curve for newer players.